// Best-effort colors sampled off the two logo screenshots you shared —
// nudge these two hex values if they're off and every use of the mark
// updates together. If you have the real vector file (SVG/AI/PDF) or the
// brand's actual hex codes, send them and I'll swap these for the exact
// values instead of an eyeballed approximation.
export const BRAND_DARK = "#171717";
export const BRAND_BLUE = "#3E7C99";
